using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class EnemyLoaderManager : MonoBehaviour
{

    Stack<string> lastEnteredRooms;

    [SerializeField] int roomsEnteredBeforeSpawn;

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
